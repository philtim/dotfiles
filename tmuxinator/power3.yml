# /Users/e-timmaphi/.config/tmuxinator/power3.yml

name: power3
root: ~/Development/Siemens/Power3

windows:
  - editor:
      layout: main-horizontal
      panes:
        - code .
        - git fetch && gs
        - glp10
        - cd source/s3 && yarn run <%= @args[0] %>
